# My Developer's Configuration

## Usage
- Clone this repository
- run the `install.sh` script (might need to give permissions with chmod)
- fix things along the way, because it's probably pretty broken
